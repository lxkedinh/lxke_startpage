import styled from "styled-components";

export const StyledClock = styled.p`
  color: white;
  font-size: 16px;
  font-family: "haxrcorp";
  width: 155px; /* static width prevents font from blurring when clock updates */
  position: relative;
  left: 4px;
  align-self: flex-start;
`;
